@model GenericAppDLL.Models.ViewModels.ViewFriendRequestsViewModel

<partial name="_SettingsImplementation" model="@Model.settings"></partial>

<div class="row">

    <div class="col-2">
    </div>
    <div class="col-8">

        @foreach (var item in Model.users)
        {
            <partial name="_FriendRequest" model="item" />
        }

    </div>
    <div class="col-2">
    </div>
</div>