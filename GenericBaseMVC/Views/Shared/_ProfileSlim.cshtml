@model GenericAppDLL.Models.ViewModels.CustomerViewModel

<div class="d-flex">

    <div class="card card-text" style="width:27em;" >
        <div class="row">
            <div class="col-4">
                <img class="card-img" src="~/profileimage.png" alt="Card image cap">
            </div>
            <div class="col-8">
                <div class="">
                    <div class="">
                        <h3 class="Card-Header"> @Model.CustomerName</h3><br />
                        <small>@Model.CustomerEmail  </small><br />
                        <small>@Model.ModelGUID  </small><br />
                    </div>
                    <span class="d-flex">
                        <form asp-action="ViewProfile" asp-controller="Profile" asp-route-email="@Model.ModelGUID">
                            <input asp-for="ModelGUID" hidden />
                            <button type="submit" asp-action="ViewProfile" asp-controller="Profile" asp-route-id="@Model.ModelGUID" class="btn btn-success"><i class="fas fa-user"></i>@*View Profile*@</button>
                        </form>
                        <a asp-action="SendDirectMessage" asp-controller="Chat" asp-route-id="@Model.ModelGUID" class="btn btn-primary"><i class="fa-solid fa-message"></i>@*Message*@</a>
                       @if (Model.IsFriend == 99 || Model.IsFriend == 3)
                        {
                            <a asp-action="SendFriendRequest" asp-controller="Profile" asp-route-id="@Model.ModelGUID" class="btn btn-secondary"><i class="fas fa-user-plus"></i>@*Add Friend*@</a>
                        }

                        @if (Model.IsFriend == 2)
                        {
                            <a asp-action="SendFriendRequest" asp-controller="Profile" asp-route-id="@Model.ModelGUID" class="btn btn-Warning"><i class="fas fa-user-minus"></i>@*Add Friend*@</a>
                        }
                        @if (Model.IsFriend == 1)
                        {
                            <a asp-action="AcceptFriendRequest" asp-controller="Profile" asp-route-id="@Model.ModelGUID" class="btn btn-success AddItemToCart"><i class="fa-regular fa-circle-check"></i></a>
                            <a asp-action="DeclineFriendRequest" asp-controller="Profile" asp-route-id="@Model.ModelGUID" data-target="#Modal" class="btn btn-danger AddItemToCart"><i class="fa-regular fa-circle-xmark"></i></a>
                        }
                    </span>
                </div>
            </div>
        </div>
    </div>

</div>